<!DOCTYPE html>
<html>
<head>
  <title>App</title>
  <%= stylesheet_link_tag    "application", "bootstrap", "pagination",media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<% if flash[:error] != nil %>
	    <div class="alert alert-danger">
	        <%= flash[:error] %>
	    </div>
    <% end %>

    <% flash.keys.each do |key| %>
		<div class="flash flash-<%= key %>">
		    <%= flash[key] %>
		</div>
	<% end %>

	<div style='margin-top: 10px; margin-left:130px;width:500px'>
	  <% if @author == nil %>
	    You must <a href="/login">login</a> to edit or create a new post.</a>
	  <% else %>
	    You are logged in as <b><%= @author.full_name %></b>.
	    <a href="/new">Write new post</a> or <a href="/logout">logout</a>
	  <% end %>
	</div>
	<hr>

	<%= yield %>

</body>
</html>
